<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Store Directory
    |--------------------------------------------------------------------------
    |
    | Where the per-form webhook configuration is stored. On a multi-site
    | installation each site gets its own subdirectory.
    |
    */

    'store_directory' => base_path('resources/zapier'),

];
